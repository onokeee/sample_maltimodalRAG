# venv環境を使ったセットアップガイド

## 🚀 Windows での起動方法

### 1. セットアップ（初回のみ）

プロジェクトディレクトリで以下を実行：

```cmd
setup.bat
```

このスクリプトが自動的に：
- Python仮想環境（venv）を作成
- pipをアップグレード
- 必要なパッケージをインストール

### 2. アプリの起動

```cmd
start.bat
```

または、手動で起動する場合：

```cmd
venv\Scripts\activate
streamlit run app.py
```

### 3. 仮想環境の終了

```cmd
deactivate
```

---

## 🍎 Mac/Linux での起動方法

### 1. スクリプトに実行権限を付与（初回のみ）

```bash
chmod +x setup.sh start.sh
```

### 2. セットアップ（初回のみ）

```bash
./setup.sh
```

### 3. アプリの起動

```bash
./start.sh
```

または、手動で起動する場合：

```bash
source venv/bin/activate
streamlit run app.py
```

### 4. 仮想環境の終了

```bash
deactivate
```

---

## 📋 仮想環境のメリット

- **依存関係の分離**: プロジェクトごとに独立したパッケージ環境
- **バージョン管理**: 異なるプロジェクトで異なるバージョンを使用可能
- **クリーンな環境**: システムのPython環境を汚さない
- **再現性**: requirements.txtで環境を再現可能

---

## 🔧 トラブルシューティング

### セットアップエラー

#### Pythonが見つからない

```cmd
# Pythonのバージョン確認
python --version

# Python 3.8以上が必要です
```

#### pipのエラー

```cmd
# pipを手動でアップグレード
python -m pip install --upgrade pip

# 再度セットアップ実行
setup.bat  # Windows
./setup.sh # Mac/Linux
```

### 起動エラー

#### 仮想環境が見つからない

```cmd
# セットアップを再実行
setup.bat  # Windows
./setup.sh # Mac/Linux
```

#### パッケージが見つからない

仮想環境をアクティベートしてから、手動インストール：

```cmd
# Windows
venv\Scripts\activate
pip install -r requirements.txt

# Mac/Linux
source venv/bin/activate
pip install -r requirements.txt
```

---

## 🗑️ 仮想環境のリセット

### Windows
```cmd
rmdir /s /q venv
setup.bat
```

### Mac/Linux
```bash
rm -rf venv
./setup.sh
```

---

## 📦 パッケージの追加

新しいパッケージを追加する場合：

```cmd
# 仮想環境をアクティベート
venv\Scripts\activate  # Windows
source venv/bin/activate  # Mac/Linux

# パッケージをインストール
pip install パッケージ名

# requirements.txtに保存
pip freeze > requirements.txt
```

---

## 🔍 仮想環境の確認

### アクティベート状態の確認

コマンドプロンプト/ターミナルの先頭に `(venv)` が表示されていればOK：

```
(venv) C:\MCP_Learning\chapter01\multimodal_rag>
```

### インストール済みパッケージの確認

```cmd
pip list
```

### 仮想環境のPythonパスを確認

```cmd
# Windows
where python

# Mac/Linux
which python
```

仮想環境内のPythonが表示されていればOK。

---

## 💡 ヒント

### IDEでの設定

#### VSCode
1. `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)
2. "Python: Select Interpreter" を選択
3. `./venv/Scripts/python.exe` (Windows) または `./venv/bin/python` (Mac/Linux) を選択

#### PyCharm
1. Settings → Project → Python Interpreter
2. 歯車アイコン → Add
3. Existing Environment → venv内のPythonを選択

---

## 📖 参考コマンド一覧

### Windows

| 操作 | コマンド |
|------|----------|
| セットアップ | `setup.bat` |
| アプリ起動 | `start.bat` |
| 仮想環境アクティベート | `venv\Scripts\activate` |
| 仮想環境終了 | `deactivate` |
| パッケージ一覧 | `pip list` |

### Mac/Linux

| 操作 | コマンド |
|------|----------|
| セットアップ | `./setup.sh` |
| アプリ起動 | `./start.sh` |
| 仮想環境アクティベート | `source venv/bin/activate` |
| 仮想環境終了 | `deactivate` |
| パッケージ一覧 | `pip list` |

---

**セットアップが完了したら、`QUICKSTART.md` を参照してアプリの使い方を確認してください！**
